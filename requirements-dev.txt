# Development, test and build tooling - never shipped inside the binary.
# Install the hashed runtime lock first; do not re-resolve its packages here.
# python -m pip install --require-hashes -r requirements.lock
pytest==9.1.1
pyinstaller==6.22.3
pyinstaller-hooks-contrib==2026.7
